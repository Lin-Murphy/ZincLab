# Self-hosted font handoff

The first build uses a robust local system sans-serif stack and makes no third-party font requests. Add licensed WOFF2 files here, then declare them in `src/styles/global.css` after the final Latin and Chinese font families are approved.
